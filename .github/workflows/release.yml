name: Build and Release VSCode Extension

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
    types: [ closed ]
  workflow_dispatch: # Allow manual trigger

jobs:
  check-version:
    runs-on: ubuntu-latest
    outputs:
      should-release: ${{ steps.version-check.outputs.should-release }}
      package-version: ${{ steps.version-check.outputs.package-version }}
      latest-version: ${{ steps.version-check.outputs.latest-version }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get package version
        id: package-version
        run: |
          PACKAGE_VERSION=$(node -p "require('./package.json').version")
          echo "version=$PACKAGE_VERSION" >> $GITHUB_OUTPUT
          echo "Package version: $PACKAGE_VERSION"

      - name: Get latest release version
        id: latest-release
        run: |
          LATEST_VERSION=$(curl -s "https://api.github.com/repos/${{ github.repository }}/releases/latest" | jq -r '.tag_name // "v0.0.0"' | sed 's/^v//')
          echo "version=$LATEST_VERSION" >> $GITHUB_OUTPUT
          echo "Latest release version: $LATEST_VERSION"

      - name: Compare versions
        id: version-check
        run: |
          PACKAGE_VERSION="${{ steps.package-version.outputs.version }}"
          LATEST_VERSION="${{ steps.latest-release.outputs.version }}"
          
          echo "package-version=$PACKAGE_VERSION" >> $GITHUB_OUTPUT
          echo "latest-version=$LATEST_VERSION" >> $GITHUB_OUTPUT
          
          if [ "$PACKAGE_VERSION" != "$LATEST_VERSION" ]; then
            echo "should-release=true" >> $GITHUB_OUTPUT
            echo "✅ Version changed: $LATEST_VERSION → $PACKAGE_VERSION"
            echo "Will proceed with build and release"
          else
            echo "should-release=false" >> $GITHUB_OUTPUT
            echo "⏭️ Version unchanged: $PACKAGE_VERSION"
            echo "Skipping build and release"
          fi

  build-and-release:
    needs: check-version
    if: needs.check-version.outputs.should-release == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install VSCE
        run: |
          # Install latest version with better Node.js compatibility
          npm install -g @vscode/vsce@latest
          # Verify installation
          vsce --version

      - name: Lint code
        run: npm run lint

      - name: Compile TypeScript
        run: npm run compile

      - name: Package extension
        run: |
          # Package extension with explicit output name
          vsce package --out extension.vsix || {
            echo "Failed with vsce package, trying alternative approach..."
            # Alternative: use npx with latest version
            npx @vscode/vsce@latest package --out extension.vsix
          }
          # Verify the file was created
          ls -la extension.vsix
          # Set filename for later steps
          echo "VSIX_FILENAME=extension.vsix" >> $GITHUB_ENV

      - name: Create Release
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: v${{ needs.check-version.outputs.package-version }}
          name: Release v${{ needs.check-version.outputs.package-version }}
          body: |
            ## VSCode Zotero Extension v${{ needs.check-version.outputs.package-version }}
            
            ### Changes
            - Version bump from v${{ needs.check-version.outputs.latest-version }} to v${{ needs.check-version.outputs.package-version }}
            
            ### Installation
            Download the `.vsix` file and install it in VSCode using:
            ```
            code --install-extension extension.vsix
            ```
            
            Or install via VSCode UI: Extensions → Install from VSIX...
          files: |
            extension.vsix
          draft: false
          prerelease: false

      - name: Publish to VSCode Marketplace (Optional)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        continue-on-error: true
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}
        run: |
          if [ -n "$VSCE_PAT" ]; then
            echo "🚀 Publishing to VSCode Marketplace..."
            vsce publish -p $VSCE_PAT || npx @vscode/vsce@latest publish -p $VSCE_PAT
          else
            echo "📝 To publish to VSCode Marketplace:"
            echo "1. Get Personal Access Token from: https://dev.azure.com/"
            echo "2. Add it as repository secret named 'VSCE_PAT'"
            echo "3. Re-run this workflow or push a new commit"
          fi

  notify-skip:
    needs: check-version
    if: needs.check-version.outputs.should-release == 'false'
    runs-on: ubuntu-latest
    steps:
      - name: Skip notification
        run: |
          echo "🔄 Workflow completed - no release needed"
          echo "📦 Current version: ${{ needs.check-version.outputs.package-version }}"
          echo "🏷️ Latest release: ${{ needs.check-version.outputs.latest-version }}"
          echo "💡 To trigger a release, update the version in package.json"